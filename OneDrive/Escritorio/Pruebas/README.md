Hola, mi primer archivo para github, probando git bash en windows.

git init 
git add .
git commit -m "trazabilidad"
git branch -M master 
git remote origin MikeSSM/Prueba
git push

git rm nombre del archivo 
git log --oneline
git checkout id -- nombre del archivo
git switch master
git add . nombre del archivo 
git commit -m "recuperación"
git push